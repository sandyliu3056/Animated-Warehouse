# Animated Warehouse — 部署說明

版本：**v256**　　打包日期：2026-08-21

---

## 這包裡有什麼

```
index.html                     ← 整個 app，單一檔案
sw.js                          ← service worker
manifest.webmanifest
icon-180-22.png                ← APP 圖示，四個尺寸
icon-192-22.png
icon-512-22.png
icon-maskable-512-22.png
cover-og.png / cover-square.png
Animated_Warehouse_Manual.pdf  ← 使用說明手冊，頂欄那顆鍵開的就是它
verify.js                      ← 驗證腳本，不會被載入，可以不上傳
.nojekyll
```

檔案平鋪在根目錄。

**`Animated_Warehouse_Manual.pdf` 一定要跟 `index.html` 放同一層。**
頂欄的說明鍵走相對路徑 `./Animated_Warehouse_Manual.pdf`——相對路徑在
GitHub Pages 的子路徑（`/Animated-Warehouse/`）和 Vercel 的根目錄都會對；
寫成開頭一條斜線的 `/Animated_...`，在 GitHub Pages 上會 404。
檔名要改的話，`index.html` 裡的 `AW_MANUAL_URL` 要跟著改。

---

## 上傳的三條規則

**一、`sw.js` 的 `BUILD` 一定要跟著換。**

快取名稱綁在這個字串上。它沒變的話，service worker 會認為快取還是新的，
同仁的瀏覽器一直拿舊版，你上傳了也沒人看得到。

目前是 `2026-08-21-v256`。

**二、先確定 `index.html` 上去了，再推 `sw.js`。**

反過來的話，`sw.js` 已經宣告新版本、`index.html` 還是舊的，
使用者會拿到一份版本對不上的快取。

**三、換圖示就要換檔名，而且四個地方一起換。**

`manifest.webmanifest`、`sw.js` 的 `SHELL` 清單、`index.html` 的兩個 `<link>`。
**漏掉任何一個，`cache.addAll()` 會整包失敗，service worker 就永遠裝不起來**——
不是圖示不見而已，是整個離線功能失效。

圖示的檔名尾碼每次遞增（目前 `-22`），不要沿用舊檔名。

---

## 手機看不到新版時

只按重新整理沒有用。已經加到主畫面的 App 有自己一份 service worker，
要等舊的換代才會生效，而 iOS 常常要整個關掉再開。最快的做法是三步：

1. 長按主畫面的捷徑 → 刪除
2. 設定 → Safari → 進階 → 網站資料 → 刪除 `sandyliu3056.github.io`
3. 用 Safari 重開 → 分享 → 加入主畫面

**圖示一定要走完這三步**，網頁內容則通常重開就會更新。

---

## 怎麼確認手上是哪一版

**長按頁尾那一條兩秒**，會跳出版本號與目前主題，兩秒後自己淡出。

版本號平常不顯示在畫面上。加這個手勢是因為「改了到底有沒有生效」
不該靠猜——看到的不是新版號，問題就在快取或開錯檔案，不在程式。

---

## 四套主題

| 主題 | 用途 | 特徵 |
|---|---|---|
| Marigold | 對帳、報表 | 暖棕金，預設之外最中性的一套 |
| Barnaby | 投影帶班 | 牛皮紙深褐，按鍵回饋最克制 |
| Poppy | 給新人、展示 | 馬卡龍粉彩，手寫字，**預設** |
| Doodle | 手繪教材 | 牛皮紙配歪框線，按鈕是手繪邊 |

主題只換外觀與練習題庫，不影響計費邏輯與規則。
切換在頂欄的下拉選單，選擇會記住。

**練習題庫跟著主題走**：Barnaby 出現場作業題，其餘三套出帳單題。

---

## v256 改了什麼

**頂欄多一顆「使用說明」鍵。** 進主畫面後在標題右邊，按了另開一個分頁載
PDF 手冊。三種語言都有字（使用說明／Manual／Manual），跟著頂欄的語言選單換。

沒有做成 app 內的檢視面板，是因為之前回報過「手機版打開 PDF 沒地方按叉叉」——
交給瀏覽器自己的分頁，關閉、捲動、下載、列印都是系統本來就有的，不用自己刻。

**顏色的處理。** 這顆鍵不寫死底色。頂欄四套主題差很多——Poppy 是淺色底、
其餘三套是深色底——照 `#sfx` 那樣寫死 `var(--md)` 的話，Poppy 上文字對比
只有 **2.63**，字會糊在按鍵上。所以改成透明底＋細框，框色與字色都取
`BAR_INK` 那一組（跟標題同一組，本來就是為了「壓在頂欄上要看得清楚」調的），
由 `paintBarInk()` 依主題寫成行內樣式。十二種組合（四主題 × 三運輸商）
實測最低 **6.40**、最高 9.65。

**窄螢幕（≤440px）只留圖示。** 頂欄在那個寬度本來就要換行，
再多一顆帶字的鍵會把時區和語言選單擠到沒寬度。

**自動導覽多一站。** 說明鍵沒人看到就等於沒有，導覽第 10 站會把它圈出來。

**`sw.js` 修一條：`.pdf` 不再走「文件」那條分支。** 另開分頁載 PDF 時
`req.mode` 就是 `"navigate"`，會被判成文件，離線時的 fallback 會回
`index.html`——分頁網址是 `.pdf`、內容卻是整個 app，看起來像檔案壞掉。
現在走 stale-while-revalidate：開過就有快取可以離線再開，沒開過就照實回
網路錯誤，由瀏覽器顯示自己的離線頁。

---

## 這一版與 v192 之間改了什麼

**主題從一套變四套。** 每一套有自己的色票、中文字體、按鍵回饋符號，
切換運輸商時會有過場動畫。

**練習分頁簡化成一題四選項一顆鍵。** 題庫全面改寫為書面語，
並修掉「正解永遠是最長那一個」這類會讓人猜到答案的問題。

**人物、貓狗換成手繪造型**，與 Doodle Warehouse 同一套繪圖程式。
貓改成走貓砂盆，狗維持原地；兩者在吃飯、打鬧、被摸時都不會觸發。

**點畫布上的貓狗會講話**，台詞輪流換，狗貓各一組音效。

**橫向自動全螢幕只套用在主畫面。** 其他分頁是要捲動的內容，
自動放大會把正在讀的東西蓋掉。

**APP 圖示重畫**，主角改用 App 自己的布偶貓與柯基，構圖是夜倉庫的紙箱。

---

## 已知的限制

**Poppy 的手寫字型是外連 Google Fonts。** 離線或連不到時字體退回本機圓體，
介面不會壞，只是少了手寫感。

**`index.html` 約 880 KB。** 推上去偶爾會收到 503 或 429，重推即可，不用改東西。

---

## 驗證過的項目

| 項目 | 結果 |
|---|---|
| 四套主題各跑完整流程（封面 → 主畫面 → 六個分頁 → 練習） | 0 失敗 |
| 轉向行為（進出全螢幕、切分頁、導覽期間） | 6 項全過 |
| 頂欄與動作鍵的文字對比 | 全部 4.5 以上 |
| 收工送別（四種寵物組合各跑 450 幀） | 0 例外 |
| 貓砂盆流程（go → sit → done → back） | 完整 |
| 人物造型 14 髮型 × 9 服裝 | 126 組全部畫得完 |
| 頁面 JS 例外 | 0 |
| 說明鍵：三語文字與 tooltip | 6 項全過 |
| 說明鍵：12 種主題 × 運輸商組合的對比 | 全部 6.40 以上 |
| 說明鍵：點下去另開分頁、原分頁不跳走 | 過 |
| 說明鍵：390px 寬不溢出、字自動收起 | 過 |
| 說明鍵：GitHub Pages 子路徑下相對路徑正確 | 過 |
| `sw.js` 的 `.pdf` 分支判斷 | 4 種請求全對 |
