# Animated Warehouse — 部署說明

版本：**v189**　　打包日期：2026-08-17

## 這包裡有什麼

```
index.html                    ← 整個 app，單一檔案（唯一換掉的東西）
sw.js                         ← service worker，只改了 BUILD
manifest.webmanifest          ← 原樣
icon-180-11.png               ← 你原本的黃昏版圖示，原樣保留
icon-192-11.png
icon-512-11.png
icon-maskable-512-11.png
cover-og.png / cover-square.png ← 原樣保留
.nojekyll
```

檔案平鋪在根目錄，沿用你站台原本的結構。

## 圖示沒有換

上一包我用程式重新產生了圖示，換掉了你原本那組黃昏天空的貓狗——那是錯的。
這一包**四個圖示和兩張封面圖完全沿用你原本的檔案**，一個位元組都沒動，
檔名也還是 `-11`。

因為圖示沒變，這次不需要改檔名，也不用擔心圖示快取。

## 怎麼上傳

把 `index.html` 和 `sw.js` 覆蓋上去就好。其餘檔案跟你站上的一模一樣，
但整包覆蓋也不會有問題。

## 一件必須做的事

**`sw.js` 一定要跟著換。**

裡面的 `BUILD` 從 `2026-08-17-a` 改成了 `2026-08-17-v189`。快取名稱綁在它上面
（`CACHE = "aw-" + BUILD`），這個字串沒變的話，service worker 會認為快取還是新的，
同仁的瀏覽器會一直拿舊版，你上傳了也沒人看得到。

## 為什麼你之前轉向沒反應

**你手機上跑的版本裡根本沒有這段程式。** 我查了你上傳的 v172 和站台包裡的
`index.html`，「橫向自動全螢幕」的程式碼是 0 個引用——那是後來才加的，
所以不管寫得對不對，舊版都不會有反應。要上傳新版才會生效。

順帶也把偵測方式改得更可靠，因為原本那版在真的 iPhone 上不一定準：

- **`orientationchange` 在新版 iOS Safari 已標記淘汰**，可能不觸發。
  改成同時監聽 `screen.orientation`、`resize`、`visualViewport`，
  再加上從背景切回前景時也檢查一次。
- **轉向的當下視窗尺寸還沒更新**，只檢查一次會判斷成「還是直向」。
  改成轉向後連續檢查五次（0／80／200／400／700 毫秒）。
- **iOS 的網址列會收合**，同一支手機橫向時 `innerHeight` 會在 350～420 之間跳，
  用它當門檻會時靈時不靈。改成看短邊，並加上「觸控裝置」這個條件——
  手指操作的才算，滑鼠的裝置不管視窗拉多扁都不會誤觸發。

## 這一版改了什麼

**橫向下的動作鍵縮小一階。** 十六顆按鈕在橫向排不完、本來就要捲，但原本最後一顆
被右緣的捲動提示壓掉一半，看起來像壞掉。縮小之後 iPhone 16 Pro 橫向能完整看到
**13 顆**（原本 11 顆），第 14 顆露出一半帶著 `››`，那才是「還可以往右捲」該有的樣子。

**切分頁不再掉出全螢幕。** 換頁會重建畫布，全螢幕的版面跟著被重設，所以
「橫著切到小遊戲再切回主動畫」會掉出全螢幕。現在換頁後會重新檢查一次方向。

實機尺寸驗過：iPhone 16 Pro（874×402）、16 Pro Max、iPhone 16、17 Pro，
轉橫都進全螢幕、切遍五個分頁都保持、畫布比例維持 1.607。
平板和桌機不觸發——把視窗拉寬也是 landscape，那不該整頁變全螢幕。

## 順手修掉一個舊問題

`index.html` 裡的 `apple-touch-icon` 指向 `icons/icon-180.png`，但你的站是平鋪的、
**沒有 `icons/` 這個資料夾**——那兩個連結一直是 404，iOS 加到主畫面時抓不到指定圖示。
已改成平鋪的檔名。

## 驗證過的項目

| 項目 | 結果 |
|---|---|
| HTTP 404／5xx | 0 |
| service worker 註冊與接管 | 成功 |
| manifest 解析、四個圖示都回 200 | 成功 |
| 斷網重整仍可開啟 | 成功 |
| 頁面 JS 例外 | 0 |
